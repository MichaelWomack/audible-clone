export enum Collection {
    AUDIO = 'audio'
}

export enum Routes {
    HOME = '/home',
    ADD_AUDIO = '/home/add-audio',
    SETTINGS = '/settings'
}

export enum BannerType {
    INFO = 'info',
    SUCCESS = 'success',
    ERROR = 'error',
    WARNING = 'warning'
}