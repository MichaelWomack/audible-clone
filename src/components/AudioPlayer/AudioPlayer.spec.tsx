import * as React from 'react';

import AudioPlayer from './index';
import { AudioPlayerProps, AudioPlayerState } from './index';
import { Audio, AudioBook } from '../../model/audio';
import { shallow, ShallowWrapper } from 'enzyme';

describe('<AudioPlayer/>', () => {
    let wrapper: ShallowWrapper;
    let audioPlayerProps: AudioPlayerProps;
    let audioPlayerState: AudioPlayerState;

    // it('renders successfully', () => {
    //     const audio: Audio = {
    //         downloadUrl: 'https://fake-path-to-book',
    //         title: 'A Game of Thrones',
    //         currentTime: 0,
    //         lastPlayed: null,
    //     }
        
    //     wrapper = shallow(<AudioPlayer audio={audio} isPlaying/>);
    //     expect(wrapper.exists()).toBe(true);
    // });

    it("", () => {
        
    })
});
